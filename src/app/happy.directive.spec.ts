import { HappyDirective } from './happy.directive';

describe('HappyDirective', () => {
  it('should create an instance', () => {
    const directive = new HappyDirective();
    expect(directive).toBeTruthy();
  });
});
